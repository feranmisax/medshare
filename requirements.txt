# Cloud (deployed app) dependencies only — the running app reads results from the DB
# and displays them. Heavy ML libs (xgboost, scikit-learn, statsmodels) are NOT needed
# here; they're only used for local model training/pipeline. See requirements-full.txt.
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5
SQLAlchemy==2.0.31
psycopg2-binary==2.9.9
python-dotenv==1.0.1
PuLP==2.8.0
streamlit==1.37.0
plotly==5.23.0
passlib==1.7.4
reportlab==4.2.2
